services:
  healthchecker:
    build: .
    container_name: healthchecker
    volumes:
      - healthchecker-data:/app/data
    env_file:
      - .env
    ports:
      - "8081:8081"
    restart: unless-stopped


volumes:
  healthchecker-data: